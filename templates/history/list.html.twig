{% extends 'base.html.twig' %}

{% block title %}{{'messages.appTitle' | trans }}{% endblock %}

{% block message %}
    {% for msg in app.session.flashBag.get('success') %}
	<div class="alert alert-success margin-top-13">
	    {{ msg | trans}}
	</div>
    {% endfor %}
    {% for msg in app.session.flashBag.get('error') %}
	<div class="alert alert-danger margin-top-13">
	    {{ msg | trans}}
	</div>
    {% endfor %}
{% endblock %}

{% block body %}
    <main class="container-fluid">
	<div class="container-fluid">
		{% if histories is defined and histories|length > 0 %}
			<table id="taula" class="table bootstrap-table mt-3">
				<thead>
					<tr class="row">
						<th data-sortable="true">{{"history.id" | trans | capitalize}}</th>
						<th data-sortable="true">{{"history.senderAcoount" | trans | capitalize}}</th>
						<th data-sortable="true">{{"history.date" | trans | capitalize}}</th>
						<th data-sortable="true">{{"history.rctpNameNumber" | trans | capitalize}}</th>
						<th data-sortable="true">{{"history.text" | trans | capitalize}}</th>
						<th data-sortable="true">{{"history.ip" | trans }}</th>
						<th data-sortable="true">{{"history.status" | trans | capitalize}}</th>
						<th data-sortable="true">{{"history.esUnicode" | trans | capitalize}}</th>
					</tr>
				</thead>
				<tbody>
					{% for history in histories %}
					<tr>
						<td>{{history.id}}</td>
						<td>{{history.senderAccount}}</td>
						<td>{{history.date | date('Y-m-d H:i:s') }}</td>
						<td>{{history.rctpNameNumber}}</td>
						<td>{{history.text}}</td>
						<td>{{history.ip}}</td>
						<td>{{history.status}}</td>
						<td>{{history.esUnicode}}</td>
					</tr>
					{% endfor %}
				</tbody>
			</table>
		{% else %}
			<div class='border border-secondary rounded-sm text-secondary bg-light text-center p-2'>{{"messages.results0" | trans }}</div>
		{% endif %}
	</div>
    </main>
{% endblock %}

{% block stylesheets %}
    {{ parent() }}
	{{ encore_entry_link_tags('history_list_view') }}
{% endblock %}

{% block javascripts %}
    {{  parent() }}
	{{ encore_entry_script_tags('history_list_view') }}
{% endblock javascripts %}